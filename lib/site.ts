// Site base path. MUST match `basePath` in next.config.ts.
// Next.js prefixes its own generated assets (chunks, fonts, favicon) with this
// automatically, but manual references (next/image `src`, plain <a href> to
// /cv.pdf) need it applied by hand.
export const BASE_PATH = "/academic-homepage";

export const SITE_URL = "https://ZDDWLIG.github.io" + BASE_PATH;
